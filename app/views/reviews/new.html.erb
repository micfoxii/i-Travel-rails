<h1> New Review </h1>

<p>
<%= "Select Country, Select State if Country United States" %>
<%= form_for(@review) do |f|%>
    <p> Select a city: </p>
    <div>
    <%= f.collection_select :city_id, City.all, :id, :name %>
    </div>

    // <div>
    // <%= f.collection_select :state_id, State.all, :id, :name %>
    // </div>

    // <div>
    // <%= f.collection_select :country_id, Country.all, :id, :name %>
    // </div>

    <p> Create a new city: </p>
    <div>
        <%= f.fields_for :city do |city_attributes| %>
            <%= city_attributes.text_field :name, placeholder: "City Name" %>
        <% end %>
    </div>

    // <div>
        // <%= f.fields_for :state do |state_attributes| %>
        //     <%= state_attributes.text_field :name, placeholder: "State Name" %>
    //     <% end %>
    // </div>

    // <div>
    //     <%= f.fields_for :country do |country_attributes| %>
    //         <%= country_attributes.text_field :name, placeholder: "Country Name" %>
    //     <% end %>
    // </div>

    <div>
        <%= f.label :title %>
        <%= f.text_field :title %>
    </div>

    <div>
        <%= f.label :content %>
        <%= f.text_area :content %>
    </div>

    <%= f.submit %>
<% end %>


<%= "Rating" %><br>
<%= "Title" %>
<%= "Content" %><br>

<%= "TODO Lodging, Restaurant, Attraction" %>

</p>