<h1> Create a New Country: </h1>

<%= form_for(@country) do |f| %>
    <div>
    <%= f.label :name, 'Country Name: ' %>
    <%= f.text_field :name %>
    </div>

    <%= f.submit 'Create Country' %>
<% end %>