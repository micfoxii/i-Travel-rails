<h1><%= @country.name %></h1>

<% @country.cities.each do |city| %>
    <%= link_to city.name, city_review_path(@city) %> 
    - number of reviews: 
    <%= city.reviews.count %><br>
    <%= city.average_city_rating%>
<% end %>